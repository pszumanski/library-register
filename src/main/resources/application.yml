spring:
  datasource:
    url: jdbc:mysql://localhost:3307/LIBRARY_REGISTER
    username: "root"
    password: "1709"

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
