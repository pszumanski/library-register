spring:
  datasource:
    url: jdbc:mysql://localhost:3307/library_register
    username: "root"
    password: "root"

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
